/**
 * Number of children shown per page in the registry.
 *
 * Shared by the data layer (`getChildrenProfiles`) and the `Pagination` UI so
 * the fetched range and the rendered controls never drift out of sync.
 */
export const PAGE_SIZE = 8
